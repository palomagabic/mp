<%= image_tag 'punta.jpg' %>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="logo">Mente Pequeña</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
      <%= link_to current_user.email, edit_user_registration_path %>
      <li class="nav-item">
        <a class="nav-link" href="<%=link_to destroy_user_session_path, method: :delete %>">Salir</a>
      </li>
      <% else %>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_user_registration_path%>">Registrarse</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_user_session_path%>">Ingresar</a>
      </li>
      <% end %>
    </ul>
  </div>
</nav>
